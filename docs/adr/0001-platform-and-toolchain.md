# ADR-0001：首要平台与工具链

- 状态：接受
- 日期：2026-07-31

## 背景

项目从零实现 Windows 输入法，需要明确首要兼容目标。

## 决策

首要支持 Windows 11 x64，使用 C++20、MSVC、CMake、vcpkg 和 TSF/COM。其他 Windows 版本后续适配，不在首个闭环承诺范围内。

## 影响

CI 和开发验证优先覆盖 Windows 11。不得无意使用无法说明最低版本要求的 API。

## 回滚或迁移

后续通过兼容性层和独立测试矩阵增加 Windows 10 支持，不改变核心输入接口。

